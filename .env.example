# ─────────────────────────────────────────────────────────────────
# Undying Scraper — environment variables
# Copy this file to .env and fill in real values before running.
# NEVER commit your actual .env file.
# ─────────────────────────────────────────────────────────────────

# Supabase project credentials
# Create a project at https://supabase.com, then find these in:
#   Project Settings > API > Project URL / anon public key
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Discord incoming webhook URL (optional — leave empty to disable alerts)
# Create at: Server Settings > Integrations > Webhooks > New Webhook
DISCORD_WEBHOOK_URL=

# Bright Data collector ID (not needed until v0.2 target site is chosen — leave empty for mock mode)
# Format: c_xxxxxxxxxxxxxxxxxx
# Get this by running: bdata scraper create "<your-target-url>" "<field description>"
# Then paste the returned ID here.
BRIGHTDATA_COLLECTOR_ID=

# Target URL for the scraper run.
# BOTH BRIGHTDATA_COLLECTOR_ID and TARGET_URL must be set for live mode.
# If either is missing, the pipeline automatically falls back to mock data.
TARGET_URL=
