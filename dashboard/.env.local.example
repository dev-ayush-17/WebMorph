# dashboard/.env.local.example
# Copy this to dashboard/.env.local and fill in real values.
# These NEXT_PUBLIC_ variables are read at build time and in the browser.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
